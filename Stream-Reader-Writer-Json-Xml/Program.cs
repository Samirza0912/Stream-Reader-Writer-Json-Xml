using System;
using System.IO;

namespace Stream_Reader_Writer_Json_Xml
{
    class Program
    {
        static void Main(string[] args)
        {
            if (!File.Exists())
            {
                File.Create());

            }
        }
    }
}
